[project]
name = "ai-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "python-dotenv>=1.2.2",
    "sentence-transformers>=5.6.1",
]
